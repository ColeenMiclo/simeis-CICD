name: build

on:
  push:
    branches:
      - main
      
  pull_request:
    branches:
      - main

jobs:
  checks:
    steps:
      - name: Check code
        run: make check

      - name: Unit tests
        run: make test
  
      